#include <stdlib.h>
#include "draw.h"
#include <time.h>

void drawPoints()
{
	glEnableClientState(GL_VERTEX_ARRAY);
	glEnableClientState(GL_COLOR_ARRAY);
	int numPoints = 5;
	int vertices[2*5];
	int colors[3*5];
	for(int i=0;i<5;i++)
	{
		vertices[i] = 
	}
	glVertexPointer();
}

